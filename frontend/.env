# Base URL for the Ocelot Gateway. In dev, vite.config.js proxies /api to this,
# so VITE_API_BASE_URL can stay empty and requests just hit /api/... on localhost:5173.
# In production, point this straight at the deployed gateway URL.
VITE_API_BASE_URL=https://localhost:5000
